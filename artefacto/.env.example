# Configuración del agente de detección de sandbox

# URL del servidor donde se enviarán los datos
# Ejemplo desarrollo local:
# SERVER_URL=http://localhost:8080/api/collect
# Ejemplo producción:
SERVER_URL=https://releases.life/api/collect

# Habilitar modo debug (1 = activado, 0 = desactivado)
DEBUG=0

# Timeout para peticiones HTTP (aumentado para payloads grandes)
TIMEOUT=120s

# ========== XSS AUDIT MODE ==========
# Activa el modo de auditoría XSS para detectar vulnerabilidades en sandboxes
# ADVERTENCIA: Solo usar con fines de investigación de seguridad y con permiso
XSS_AUDIT=false

# Servidor de callback donde se recibirán los hits de XSS
# Debe ser accesible públicamente desde los sandboxes
CALLBACK_SERVER=https://releases.life
