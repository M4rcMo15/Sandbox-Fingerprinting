# Configuración de producción
# IMPORTANTE: Cambia estos valores antes de desplegar

# Django
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_MINIMUM_50_CHARACTERS
DEBUG=False
ALLOWED_HOSTS=your-server-ip,localhost,127.0.0.1

# Base de datos (SQLite por defecto, cambiar a PostgreSQL si es necesario)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=/opt/artefacto-visualizer/db.sqlite3

# Seguridad
CSRF_TRUSTED_ORIGINS=http://your-server-ip,http://your-server-ip:8080

# Servidor
PORT=8080
WORKERS=4
